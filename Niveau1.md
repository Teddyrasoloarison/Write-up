# Niveau 1
```
Premièrement,vérifier le contenu du fichier :
ls
Ensuite, vérifier chaque contenu :
cd nom du fichier
ls nom du fichier
Mais la solution du niveau 1 se trouve dans le fichier patience :
cd patience
ls
cat SOLUTION.txt
```
